const fs = require('fs'); const triagePath = 'c:/Users/Jhon/Desktop/upa_final/src/pages/TriageRoom.tsx'; const pediaPath = 'c:/Users/Jhon/Desktop/upa_final/src/pages/PediatriaRoom.tsx'; let tCode = fs.readFileSync(triagePath, 'utf8'); let pCode = fs.readFileSync(pediaPath, 'utf8'); const tStartMarker = '<div className=\"grid grid-cols-1 lg:grid-cols-12 gap-4\">'; const tEndMarker = '{/* Classificação Final */}'; let tStart = -1; let currentIdx = 0; while (true) { const idx = tCode.indexOf(tStartMarker, currentIdx); if (idx === -1) break; if (idx > 60000 && idx < 120000) { tStart = idx; break; } currentIdx = idx + 1; } const tEnd = tCode.indexOf(tEndMarker, tStart); const pStartMarker = '<div className=\"grid grid-cols-1 lg:grid-cols-12 gap-4\">'; const pEndMarker = '{/* Classificação Final */}'; let pStart = -1; currentIdx = 0; while (true) { const idx = pCode.indexOf(pStartMarker, currentIdx); if (idx === -1) break; if (idx > 60000 && idx < 120000) { pStart = idx; break; } currentIdx = idx + 1; } const pEnd = pCode.indexOf(pEndMarker, pStart); console.log({tStart, tEnd, pStart, pEnd});
